/**
 * The student voting portal is part of this React app — there is no separate
 * mobile build to install or host. Link to this path from anywhere on the site.
 */
export const STUDENT_PORTAL_PATH = '/student-login'
